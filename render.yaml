services:
  - type: web
    name: ocr-excel-converter
    runtime: python
    buildCommand: pip install -r requirements-web.txt
    startCommand: python web_server.py
    envVars:
      - key: PORT
        value: 5000
      - key: PYTHON_VERSION
        value: "3.12.0"
